"""
One Collector per observation domain (document section 3.2).

Three at the MVP, which is the whole of section 6's scope: CPU/RAM,
containers, kernel logs. GPU, network, systemd units and the rest wait
for V2, along with the Host Model that would make their dependencies
mean something.
"""
