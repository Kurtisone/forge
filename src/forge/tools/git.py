"""
Placeholder for the git tool (see README roadmap).

Intentionally empty: it exposes no run() function, so the registry
skips it. Add a run(content: str) -> str function here when this
tool is implemented -- and add sandboxing before doing so for
shell.py / files.py, since the registry will load and dispatch to
it with no extra confirmation step.
"""
