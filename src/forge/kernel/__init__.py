"""
Forge Kernel — the architectural axis described in ARCHITECTURE.md.

Nothing in here is load-bearing yet. The orchestrator still dispatches
through forge.tools.registry exactly as before. This package is phase 1
(Primitive) of Niveau 2: the capability abstraction exists and is
inspectable, so that Niveau 3 can be added later without a rewrite.
"""
